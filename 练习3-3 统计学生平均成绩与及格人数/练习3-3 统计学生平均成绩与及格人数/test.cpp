#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
#include<stdlib.h>
int main()
{
	/*double sum = 0;
	double average = 0;
	int count = 0;
	int n, sore;
	scanf("%d", &n);
	if (n > 0)
	{
		for (int i = 0; i < n; i++)
		{
			scanf("%d", &sore);
			sum += sore;
			if (sore >= 60)
			{
				count++;
			}
		}
		average = sum / n;
	}
	printf("average = %.1f\n", average);
	printf("count = %d\n", count);*/



	system("pause");
	return 0;
}